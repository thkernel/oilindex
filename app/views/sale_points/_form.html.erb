<%= form_with(model: sale_point, local: true) do |form| %>
  <% if sale_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale_point.errors.count, "error") %> prohibited this sale_point from being saved:</h2>

      <ul>
      <% sale_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
