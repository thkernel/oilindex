<%= form_with(model: agent, local: false) do |form| %>
 

  <div class="modal-body">
    <ul class="errors">
    </ul>

  <div class="form-group">
    <%= form.label :first_name, "Prénom", class: "required" %>
    <%= form.text_field :first_name, class: "form-control", placeholder: "Prénom", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :last_name, "Nom", class: "required" %>
    <%= form.text_field :last_name, class: "form-control", placeholder: "Nom", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :gender, "Sexe", class: "required" %>
    <%= form.select :gender , GENDERS, {prompt: "Sélectionner"}, class: "form-control", style: "width: 100%", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :sale_point_id , "Station", class: "required" %>
    <%= form.collection_select :sale_point_id, @sale_points, :id, :name, {prompt: "Sélectionner"},class: "form-control", style: "width: 100%;" %>
  </div>


   </div><!-- modal body -->
  <div class="modal-footer">
    <%= link_to "Annuler", "#", class: "btn btn-secondary tx-mont tx-medium tx-11 tx-uppercase pd-y-12 pd-x-25 tx-spacing-1", data: {dismiss: "modal"} %>
    <%= form.submit  class: "btn btn-primary tx-mont tx-medium tx-11 tx-uppercase pd-y-12 pd-x-25 tx-spacing-1",  "data-loading-text"=>"Loading...", autocomplete: "off" %>
  </div>
<% end %>
