<p id="notice"><%= notice %></p>

<h1>Order Details</h1>

<table>
  <thead>
    <tr>
      <th>Uid</th>
      <th>Order</th>
      <th>Product</th>
      <th>Price</th>
      <th>Devise code</th>
      <th>Quantity</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order_details.each do |order_detail| %>
      <tr>
        <td><%= order_detail.uid %></td>
        <td><%= order_detail.order %></td>
        <td><%= order_detail.product %></td>
        <td><%= order_detail.price %></td>
        <td><%= order_detail.devise_code %></td>
        <td><%= order_detail.quantity %></td>
        <td><%= order_detail.status %></td>
        <td><%= link_to 'Show', order_detail %></td>
        <td><%= link_to 'Edit', edit_order_detail_path(order_detail) %></td>
        <td><%= link_to 'Destroy', order_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order Detail', new_order_detail_path %>
